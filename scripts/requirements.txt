# VerifAI model service (scripts/inference_server.py)
#   pip install -r scripts/requirements.txt
fastapi
uvicorn[standard]
python-multipart
pillow
numpy
torch
torchvision

# Hugging Face fallback classifier, used when models/deepfake_detector.pth is absent.
transformers

# Grad-CAM explainability. Without it the service still answers, with heatmap: null.
grad-cam

# The NPR whole-image detector runs from ONNX in both builds.
onnxruntime

# Voice. REQUIRED for /predict-audio to be correct, not optional: without librosa the
# spectrogram code falls back to a plain STFT magnitude, which is not what the voice model
# was trained on. It answers anyway, so the mismatch is invisible without this installed —
# the service attaches a loud caveat when it detects the fallback. soundfile is librosa's
# decoder for anything that is not a 16-bit WAV.
librosa
soundfile

# Face detection/cropping (MTCNN). Optional: without it the service still runs, reports
# faceDetected: null, and says so in `notes`.
facenet-pytorch

# Training only (scripts/train_deepfake_detector.py, preprocess_faces.py):
# kagglehub, pyyaml
