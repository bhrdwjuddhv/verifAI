# Torch-free runtime for the model service. Measured 205MB resident (peak 306MB) versus
# 1262MB for the torch build — the difference between fitting a 512MB instance and not.
#
# Needs models/detector.onnx + models/detector.json from export_onnx.py, and
# models/face_detection_yunet.onnx from OpenCV Zoo. The Dockerfile builds both.
fastapi
uvicorn[standard]
python-multipart
pillow
numpy
onnxruntime
opencv-python-headless

# librosa is deliberately NOT here. It pulls numba + scipy (~150MB), which is most of the
# headroom this build exists to protect.
#
# What that costs, exactly:
#   /predict-audio-window (Live Guard)  unaffected — the v2 chain does its own preprocessing
#                                       inside the ONNX graph and never imports librosa.
#   /predict-audio (file upload)        degraded — v1 falls back to a plain STFT magnitude,
#                                       which is not what the voice model was trained on. The
#                                       response carries a SERVED WITHOUT librosa caveat.
#
# Add `librosa` and `soundfile` here if file-upload voice accuracy matters more than image
# size on your instance.
